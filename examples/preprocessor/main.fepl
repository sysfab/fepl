$include std.feph
$include flags.feph

let backend = TARGET_NAME
let feature = FEATURE_LABEL
let sample = range(0, 7, 3)
let hasThree = contains(sample, 3)

$define MODE dev

$if MODE == prod ${
let apiUrl = "https://api.example.com"
}
$elif MODE == stage ${
let apiUrl = "https://stage.example.com"
}
$else ${
let apiUrl = "http://localhost:3000"
}
$fi

$undefine MODE

let modeToken = MODE

print(`target=${backend} feature=${feature}`)
print(hasThree)
print(len(sample))
